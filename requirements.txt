# Stable Diffusion Studio - Clean Requirements
# Optimized to avoid dependency conflicts during installation

# Core PyTorch ecosystem (let PyTorch manage CUDA dependencies)
torch>=2.0.0
torchvision>=0.15.0
torchaudio

# AI/ML core packages
diffusers>=0.20.0
transformers>=4.30.0
accelerate
safetensors
huggingface-hub
tokenizers

# Computer vision and image processing
pillow>=9.0.0
opencv-python
scikit-image
imageio
numpy
scipy
matplotlib

# FastAPI web framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
python-multipart

# Streamlit frontend
streamlit>=1.25.0

# Core utilities
requests>=2.28.0
tqdm
pyyaml
packaging
filelock

# Image enhancement packages (may cause conflicts - install manually if needed)
realesrgan
basicsr
gfpgan

# Development and testing
pytest
black
flake8
